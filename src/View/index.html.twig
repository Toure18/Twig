{% extends 'base.html.twig' %}

{% block content %}

    <h1>Hola les gens </h1>



    <div class="row">
    {% for product in products %}
        <div class="col-md-4">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{ product }}</h5>
                </div>
            </div>
        </div>
{% endfor %}
    </div>
{% endblock %}